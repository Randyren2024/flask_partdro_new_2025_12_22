收到，我完全理解您的核心要求：**双重多语言架构**。

在重构过程中，我将始终贯彻以下原则：

1.  **静态内容 (UI/菜单/通用文案)** -> **Flask-Babel**
    *   所有模板中的固定文本（如“联系我们”、“首页”、“版权所有”）都将严格包裹在 `{{ _('...') }}` 中。
    *   确保这些键值对被提取到 `messages.pot` 并翻译成 `zh_CN`, `ja`, `es`。

2.  **动态内容 (产品名称/描述/参数)** -> **Supabase**
    *   所有来自数据库的内容展示（如 S150 的介绍、产品列表）都将通过模型方法（如 `product.get_translated_name(get_locale())`）动态获取。
    *   前端模板将不再包含硬编码的产品文案。

### 修正后的执行计划：

1.  **清空现有模板**：删除 `templates/` 下的所有文件，彻底清除旧的硬编码包袱。
2.  **等待您的第一步指令**：例如“创建 base.html”或“创建 header.html”。
3.  **构建时即刻集成**：
    *   每当您让我创建一个 HTML 文件，我都会**立即**为其添加 Babel 标记。
    *   每当涉及数据展示，我都会预留好 Supabase 数据的接入点。

准备就绪，请确认执行清理，然后我们开始第一步！