我明白了。您不仅关心路由的稳健性，更关心**多语言和数据库在全站的统一性**。

在检查中我发现了一个**破坏稳健性**的问题：
**S150 详情页 (`s150_landing_page.html`) 目前没有完全使用 Babel 和 Supabase 的标准流程，而是混用了大量冗余的 JavaScript 来做前端翻译。这导致了代码维护困难且不规范。**

因此，我的改造计划将包含以下关键的“稳健性修复”步骤：

### **全站稳健性与视觉双重改造计划**

**第一步：代码清洗与规范化（确保稳健）**
1.  **修复 S150 页面**：
    *   **删除**：几百行冗余的 JavaScript 翻译字典 (`const translations = {...}`)。
    *   **替换**：完全改用 **Flask-Babel** (`{{ _() }}`) 和 **Supabase** (`product.get_translated_name`) 来输出内容。
    *   *目的*：确保全站多语言逻辑统一，不再有“特殊对待”的页面。

**第二步：全站视觉升级（S200 风格）**
1.  **全局样式 (`style.css`)**：
    *   引入 **Orbitron** 字体（配合中文回退策略）。
    *   定义 **霓虹青 + 深黑** 主题变量。
2.  **公共组件重构**：
    *   **Header**：改为磨砂黑玻璃背景，链接悬停发光。
    *   **Footer**：纯黑背景，极简风格。
3.  **首页 (`index.html`)**：
    *   彻底去除白色背景，应用深色科技卡片样式。

**这个方案将同时解决“代码乱”和“风格土”两个问题。**

**请确认执行。**
